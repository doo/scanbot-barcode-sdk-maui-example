<?xml version="1.0" encoding="utf-8"?>

<pages:BaseComponentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="ScanbotSDK.MAUI.Example.ClassicUI.Pages.BarcodeClassicArOverlayUpdatePage"
    xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls"
    xmlns:classicComponent="clr-namespace:ScanbotSDK.MAUI.Barcode.ClassicComponent;assembly=ScanbotSDK.Shared"
    xmlns:pages="clr-namespace:ScanbotSDK.MAUI.Example.ClassicUI"
    ios:Page.UseSafeArea="true"
    Title="Classic AR Overlay">
    <Grid RowDefinitions="7*,3*">
        <classicComponent:BarcodeScannerView
            x:Name="CameraView"
            Grid.Row="0"
            OnBarcodeScanResult="OnSelectBarcodeResult" />
        <!-- OnSelectBarcodeResult="OnSelectBarcodeResult" -->
        <StackLayout
            Grid.Row="1">
            <Button
                Text="Start AR Update"
                Clicked="StartArUpdate_Clicked"
                Margin="15,9" />
            <Label
                Text="Result"
                FontAttributes="Bold"
                FontSize="18"
                HorizontalTextAlignment="Center"
                VerticalTextAlignment="Center"
                VerticalOptions="Center"
                HorizontalOptions="Center" />
            <Label
                x:Name="ResultLabel"
                Margin="15,10"
                HorizontalTextAlignment="Center"
                VerticalTextAlignment="Center"
                VerticalOptions="Center"
                HorizontalOptions="Center" />
        </StackLayout>
    </Grid>
</pages:BaseComponentPage>